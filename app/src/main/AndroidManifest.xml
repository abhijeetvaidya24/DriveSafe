<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.drivesafe">

    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <uses-permission android:name="android.permission.BLUETOOTH" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".PropMainNewActivity"
            android:label="@string/title_activity_prop_main_new"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".UploadDetails" />
        <activity android:name=".CarOperation" />
        <activity android:name=".EditImage" />
        <activity android:name=".EditEmail" />
        <activity android:name=".EditPassword" />
        <activity android:name=".TypeSelect">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".OwnerSignUp" />
        <activity android:name=".UserLogin" />
        <activity android:name=".OwnerLogin" />
        <activity android:name=".SettingsActivity" />
        <activity
            android:name=".OwnerMainActivityMain"
            android:label="@string/title_activity_owner_main_main"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>